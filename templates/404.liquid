<section style="padding: 7rem 0; text-align: center;">
  <div class="container">
    <h1 class="section-title" style="margin-bottom: 1rem;">Página no encontrada</h1>
    <p style="color: var(--text-secondary); margin-bottom: 2rem;">El enlace que seguiste no existe o fue movido.</p>
    <a href="{{ routes.root_url }}" class="btn btn-secondary">Volver al inicio</a>
  </div>
</section>
