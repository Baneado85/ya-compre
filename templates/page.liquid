<section style="padding: 5rem 0;">
  <div class="container" style="max-width: 800px;">
    <h1 class="section-title" style="margin-bottom: 1.5rem;">{{ page.title }}</h1>
    <div style="color: var(--text-secondary); line-height: 1.7;">
      {{ page.content }}
    </div>
  </div>
</section>
